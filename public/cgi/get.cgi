#!/usr/bin/env bash
# Placeholder CGI-like endpoint for compatibility with devices making GET requests.
# This is a static text file placed at cgi/get.cgi to return a simple message.

Content-Type: text/plain

home_login endpoint placeholder
